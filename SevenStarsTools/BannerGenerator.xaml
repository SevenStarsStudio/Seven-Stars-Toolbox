<Window x:Class="SevenStarsTools.BannerGenerator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SevenStarsTools"
        
        Title="Banner Generator" ResizeMode="NoResize" Height="800" Width="600" Icon="/icon.ico" Background="#1b1a19" >
    <Grid>
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Center">Banner Generator</TextBlock>
        <Line Width="256"  Height="3" Margin="0,42,0,0" X1="0" Y1="0" X2="254" Y2="0" VerticalAlignment="Top" HorizontalAlignment="Center"></Line>

        <Button x:Name="btnOpenExplorerShieldSourceTemplate" Click="btn_openExplorer_shieldSourceTemplate" Content="Find Shield Source Template" Height="32" Width="256" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,48,0,0"/>
        <Button x:Name="btnOpenExplorerShieldTemplate" Click="btn_openExplorer_shieldTemplate" Content="Find Shield Template" Height="32" Width="256" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,48,0,0"/>
        <Image Name="shieldSourceTemplate" Height="128" Width="128" HorizontalAlignment="Left" VerticalAlignment="Top"  Margin="50,85,0,0" RenderOptions.BitmapScalingMode="HighQuality"></Image>
        <Image Name="shieldTemplate" Height="128" Width="128" HorizontalAlignment="Right" VerticalAlignment="Top"  Margin="0,85,50,0" RenderOptions.BitmapScalingMode="HighQuality"></Image>

        <Line Width="500"  Height="1" Margin="0,230,0,0" X1="0" Y1="0" X2="500" Y2="0" VerticalAlignment="Top" HorizontalAlignment="Center"></Line>
        <Button x:Name="btn_openExplorerShieldFiles" Click="btn_openExplorer_shieldFiles" Content="Find Shields" Height="32" Width="256" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,240,0,0"/>

        <Line Width="500"  Height="1" Margin="0,500,0,0" X1="0" Y1="0" X2="500" Y2="0" VerticalAlignment="Top" HorizontalAlignment="Center"></Line>

    </Grid>

    <!-- STYLE -->
    <Window.Resources>
        <Style TargetType="{x:Type Window}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Courier New"/>
            <Setter Property="FontStyle" Value="Normal"/>
        </Style>


        <Style TargetType="{x:Type Button}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#edc591"/>
            <Setter Property="Background" Value="#242221"/>
            <Setter Property="BorderBrush" Value="#43413e"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="18"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#a1805e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#43413e"/>

                    <Setter Property="Foreground" Value="#a1805e"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="#edc591" />
            <Setter Property="FontSize" Value="24" />
        </Style>

        <Style TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="#edc591" />
            <Setter Property="FontSize" Value="24" />
        </Style>

        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="#edc591" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <Style TargetType="{x:Type Line}">
            <Setter Property="Stroke" Value="#edc591" />
        </Style>

    </Window.Resources>
</Window>
